# Python 3.11 или 3.12 (НЕ 3.14 — часть пакетов ещё не поддерживает)
# Установка:  pip install -r requirements.txt
# Модели spaCy ставятся отдельно (см. README):
#   python -m spacy download en_core_web_sm
#   python -m spacy download ru_core_news_sm

# --- данные ---
# MASSIVE — parquet-нативный датасет на Hugging Face, грузится штатно
# (данные лежат на HF, без внешних серверов и скриптов).
datasets>=2.14
pyarrow>=12.0          # кэш в parquet
pandas>=2.0
numpy>=1.24

# --- классический бейзлайн (этап 4) ---
scikit-learn>=1.3      # TF-IDF + LogisticRegression
joblib>=1.3            # сохранение бейзлайна

# --- transfer learning / DistilBERT (этап 5) ---
tensorflow>=2.13       # Keras-модель, сохранение в .h5
transformers>=4.35     # DistilBERT (TF-версия)

# --- тегирование (этап 7) ---
spacy>=3.7             # NER
langdetect>=1.0.9      # определение языка документа

# --- дашборд (этап 9) ---
streamlit>=1.28
plotly>=5.17

# --- EDA и утилиты ---
matplotlib>=3.7
seaborn>=0.13
tqdm>=4.66
