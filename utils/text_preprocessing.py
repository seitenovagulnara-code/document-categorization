"""
Этапы 2-3 — Предобработка текста.

Важно: предобработка РАЗВЕТВЛЯЕТСЯ по моделям.
  - basic_clean()       — общая базовая очистка для всех (сразу после загрузки).
  - classic_preprocess()— тяжёлая чистка ТОЛЬКО для бейзлайна TF-IDF
                          (нижний регистр, стоп-слова, лемматизация).
  - для DistilBERT классический препроцессинг НЕ применяется:
    у него свой субсловный токенизатор, агрессивная чистка вредит.

Заготовка: сигнатуры и порядок. Логику наполняем на этапах 2-3.
"""


def basic_clean(text: str) -> str:
    """
    Общая базовая очистка (для всех путей):
    убрать лишние пробелы, битые символы, HTML-артефакты, дубли переносов.
    """
    # TODO (этап 2): реализовать
    raise NotImplementedError


def detect_language(text: str) -> str:
    """Определить язык документа ('en' / 'ru' / ...). Нужно и для роутинга spaCy."""
    # TODO (этап 2): langdetect
    raise NotImplementedError


def classic_preprocess(text: str, lang: str) -> str:
    """
    Тяжёлый препроцессинг ТОЛЬКО для TF-IDF-бейзлайна:
    нижний регистр, удаление стоп-слов, лемматизация (по языку).
    """
    # TODO (этап 3/4): реализовать через spaCy по языку
    raise NotImplementedError
